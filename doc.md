### 🟢 提示词 1：初始化、数据模型与序列化 (为备份做准备)

**(复制以下内容发送给 AI)**

> 你现在是一位资深的 Flutter App 开发工程师。我需要开发一款名为「MeetLog (遇记)」的 App (仅支持 iOS/Android，无需考虑 Web)。
> 核心要求：**完全无后端，纯本地 Isar 数据库，并支持将数据导出为 JSON 文件备份**。
>
> 请帮我使用 `isar`, `flutter_riverpod` 和 `go_router` 搭建基础架构。
>
> 1. 请编写 `ApproachRecord` (搭讪记录) 和 `Contact` (联系人) 的 Isar 数据模型 (Collection)。
>    - `ApproachRecord` 包含字段：id (Isar的Id类型), dateTime (时间), location (地点), isSuccess (布尔值), failReason (失败原因，字符串), reflection (复盘文本)。
>    - `Contact` 包含字段：id, recordId (关联的记录ID), name (称呼), platform (平台，如微信), account (账号), impressionScore (印象分 1-5), followUpDate (跟进日期)。
> 2. **关键**：为了后续的本地备份功能，请务必为这两个 Model 编写 `fromJson` 和 `toJson` 方法，方便进行 JSON 序列化。
> 3. 提供一个 App 的底层导航 `MainScreen`，使用 `NavigationBar` (Material 3 底部导航栏)，包含三个 Tab：记录、图鉴、复盘。
> 4. 给出基础的 `pubspec.yaml` 依赖清单（包含 isar, file_picker, path_provider, freezd/json_serializable 等）。

---

### 🟢 提示词 2：本地数据库服务与「导入/导出备份」功能

**(在 AI 回复完上一步后，发送以下内容)**

> 非常好。现在我们需要编写数据访问层 `LocalDbService`，以及极其重要的**数据备份与恢复**功能。
>
> 请编写一个 `LocalDbService` 类（或者通过 Riverpod 提供），包含以下功能：
>
> 1. Isar 的初始化方法（仅针对移动端，使用 `path_provider` 获取应用文档目录）。
> 2. 基础的 CRUD 方法：新增记录 (如果成功需同时保存 Record 和 Contact)、查询所有记录、查询联系人。
> 3. **核心功能：导出备份 (exportData)**：
>    - 将数据库中所有的 `ApproachRecord` 和 `Contact` 取出，转换为 JSON 字符串。
>    - 使用 `file_picker` 插件弹出保存文件对话框，让用户将这个 JSON 字符串保存为 `meetlog_backup.json` 文件到手机本地。
> 4. **核心功能：导入恢复 (importData)**：
>    - 使用 `file_picker` 让用户选择之前备份的 JSON 文件。
>    - 读取并解析 JSON，清除当前数据库数据，将备份数据批量写入 Isar 数据库。
>
> 请给出完整的 Dart 代码，并包含必要的异常处理（如用户取消选择文件）。

---

### 🟢 提示词 3：新增搭讪页面的 UI 与输入提示词

**(在 AI 回复完上一步后，发送以下内容)**

> 现在我们来编写核心表单 UI：新增搭讪记录页面 (`AddRecordScreen`)。要求符合 App 单手操作习惯，使用 Material 3。
>
> UI 需求如下：
>
> 1. 顶部两个大面积的 Card 按钮：“✅ 成功拿到联系方式” 和 “❌ 遗憾失败”。
> 2. **点击成功时（展开表单）**：
>    - 请在输入框中加入**温柔的提示词 (HintText)**。
>    - 称呼输入框（提示：“对方的称呼或特征，如：穿白裙的女孩/Lina”）。
>    - 平台选择下拉框（微信/电话/小红书/Instagram）。
>    - 账号输入框（提示：“填入微信号或手机号”）。
>    - 地点输入框（提示：“在哪相遇的？如：星巴克/地铁2号线”）。
>    - 初印象评分（1-5星）。
> 3. **点击失败时（展开表单）**：
>    - 单选 ChoiceChip 选择失败原因（太紧张、开场白生硬、对方有伴侣、直接被拒、其他）。
>    - 多行文本框：每次即时复盘（提示：“这次哪里做的不够好？下次怎么改进？”）。
> 4. 点击保存按钮，调用 `LocalDbService` 写入本地数据库，并弹窗提示成功。

---

### 🟢 提示词 4：联系人图鉴 (App端列表)

**(在 AI 回复完上一步后，发送以下内容)**

> 接下来开发 `/contacts` 对应的「联系人图鉴」页面 (`ContactsScreen`)。这是用户搭讪成功后积累的“社交资产库”。
>
> UI与功能需求：
>
> 1. 页面顶部包含一个搜索框（按姓名/特征搜索）。
> 2. 主体是一个 `ListView.builder`。
> 3. 列表项使用美观的 `Card` 组件，显示以下信息：
>    - 对方称呼 (大字)
>    - 记录的获取时间和地点 (小字)
>    - 印象分（用星星图标展示数量）
>    - 平台图标（比如微信图标，暂时用通用 Icon 代替）。
> 4. 增加一个细节：如果当前时间距离该联系人的 `followUpDate`（后续跟进提醒日期）小于 1 天，请在卡片右上角显示一个显眼的红底白字标签：“🔔 该打个招呼啦”。
> 5. 监听 Riverpod Provider，确保新增联系人后列表会自动刷新。

---

### 🟢 提示词 5：数据统计面板 (每周/每月总结) 

**(在 AI 回复完上一步后，发送以下内容)**

> 最后，我们开发复盘核心功能：`/dashboard` 数据统计面板 (`DashboardScreen`)。此页面在 App 端需要排版紧凑。
>
> 请使用 `fl_chart` 库绘制图表：
>
> 1. 顶部使用 `SegmentedButton` 切换：【本周总结】和【本月总结】。
> 2. **成功率展示**：使用 `PieChart` (饼图)，展示成功与失败的比例，中心写上总次数（如“共15次”）。
> 3. **失败原因分布**：使用 `BarChart` (柱状图)，统计各项失败原因的频次，X轴为原因，Y轴为次数。
> 4. **复盘回顾墙**：在页面最下方，使用纵向的 `ListView` (需处理好滑动嵌套) 展示该周期内所有的**失败复盘文本 (reflection)**。每条展示时间、失败原因标签和反思内容。
> 5. 在页面的 AppBar 或右上角，放置一个 `IconButton` (设置/数据图标)，点击后弹出一个 `BottomSheet`，包含我们在第 2 步写的**“导出数据备份”**和**“导入数据恢复”**两个操作按钮。